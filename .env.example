# Database Configuration (MySQL)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=SRKA!182728
DB_NAME=bloghub
DB_PORT=3306

# Server Configuration
PORT=3000
SESSION_SECRET=bloghub_super_secret_session_key_2026

# Optional: Set to true if you want to explicitly force memory storage mode
# USE_MOCK_DB=false
